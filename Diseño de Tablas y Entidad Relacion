Cadena de cines en Perú:

-- Clientes
cliente({tipo_dni}, {n_dni}, nombres, correo, telefono)
tarjeta({numero}, {cliente.tipo_dni}, {cliente.n_dni}, ccv, vencimiento, nombres)
membresia({nombre}, costo)
miembro(cliente.tipo_dni, cliente.n_dni, membresia.nombre)
-- Locales
sede({id}, nombre, direccion,departamento, distrito,  hora_apertura, hora_cierre, n_salas)

-- Peliculas
pelicula({id}, {nombre}, {lenguaje}, fecha_adquisicion, duracion, genero.id)
genero({id}, nombre_genero, niveles_publico.id)
niveles_publico({id}, rango_edad}

-- Productos
producto({id}, nombre, precio_venta)
insumos({id}, nombre, cantidad, precio_costo)


-- Funciones de película
sala({id}, {sede.id}, {numero}, n_butacas)
funcion({fecha}, {hora}, {sala.id}, pelicula.nombre, pelicula.lenguaje)
butaca({fecha, hora, sala.id, nro_butaca, es_activo, venta_entrada.id})
-- -- Table View: Para los cajeros
funcion_butacas{funcion.fecha, funcion.hora, sala.numero, sede.direccion, pelicula.nombre, pelicula.lenguaje, venta.id, venta.cantidad_entradas}

-- Ventas
venta({cliente.tipo_dni}, {cliente.n_dni}, {sede.id}, {fecha})
venta_entrada({venta.tipo_dni}, {venta.n_dni}, {venta.id}, {funcion.fecha}, {funcion.hora} {funcion.numero}, {fila_butaca}, {columna_butaca}, cantidad_entradas)
venta_alimento({venta.tipo_dni}, {venta.n_dni}, {venta.id}, {venta.fecha}, alimento.id)



-- Trabajadores:
-- Los esquemas son cada departamento del Perú en la que se encuentran sedes del cine
CREATE SCHEMA lima;
CREATE SCHEMA arequipa;
CREATE SCHEMA tacna;
-- etc;

Colaboradores: {nro_documento, tipo_documento}, nombres, apellidos, nro_telefonico, correo, nro_cuenta
AreaTrabajo: {id}, nombre
SueldoColaborador: {nro_contrato}, AreaTrabajo.id, especialidad, Colaboradores.nro_documento, Colaboradores.tipo_documento, sueldo
Administradores: Colaboradores.{nro_documento, tipo_documento}, correo, password, nivel_permisos (range 1-3: básico, intermedio, avanzando, seguridad de vistas)


Clientes:

Cliente: {nro_documento, tipo_documento}, nombres, apellidos, nro_telefonico, correo, dirección(null), departamento (null)
Socios: {nro_documento, tipo_documento}, tipo_credencial, cantidad_entradas

Ventas:

1. Entrada
CompraEntrada{id_venta}, película, tipo_entrada, cantidad_entradas, fecha, hora_funcion, tipo_pago, monto
TipoEntrada {id_tipoEntrada}, nombre, descuento, localidad
TipoPago {id} nombre
PagoTarjeta {id, id_venta}, banco, nro_cuenta, monto

2. Productos

Alimentos: {id_alimento}, nombre, precio_venta
Promociones: {id_promocion}, precio_venta, descuento
Asientos: {id_asientos}, sala, ocupado


Clientes(tarjeta_credito
